[setup]
pkgs = ["nodejs_22_x", "npm_10", "openssl"]

[phases.setup]
nixPkgs = ["nodejs_22_x", "npm_10", "openssl"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
