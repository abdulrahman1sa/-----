[setup]
pkgs = ["nodejs-22_x", "npm-10_x", "openssl"]

[phases.setup]
nixPkgs = ["nodejs-22_x", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
